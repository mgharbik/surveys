<h1>Listing rapidfire_roles</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @rapidfire_roles.each do |rapidfire_role| %>
      <tr>
        <td><%= rapidfire_role.name %></td>
        <td><%= link_to 'Show', rapidfire_role %></td>
        <td><%= link_to 'Edit', edit_rapidfire_role_path(rapidfire_role) %></td>
        <td><%= link_to 'Destroy', rapidfire_role, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Role', new_rapidfire_role_path %>
