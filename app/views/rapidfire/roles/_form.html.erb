<%= form_for(@rapidfire_role) do |f| %>
  <% if @rapidfire_role.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rapidfire_role.errors.count, "error") %> prohibited this rapidfire_role from being saved:</h2>

      <ul>
      <% @rapidfire_role.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
